import { TestBed } from '@angular/core/testing';

// import { NewRegistrationListService } from './new-registration-list.service';

describe('NewRegistrationListService', () => {
  // let service: NewRegistrationListService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    // service = TestBed.inject(NewRegistrationListService);
  });

  it('should be created', () => {
    // expect(service).toBeTruthy();
  });
});
